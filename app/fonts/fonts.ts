import { Baloo_2 } from "next/font/google";

export const Baloo = Baloo_2({
  subsets: ["latin"], // Specify the subset for preloading
  weight: ["400", "700"], // Include bold and regular weights
});
